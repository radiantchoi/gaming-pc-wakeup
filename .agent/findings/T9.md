# T9 — `GET /status` (implemented and verified by orchestrator)

- Performed by: orchestrator (Claude Fable 5.1) directly, at the user's request
- Completed: 2026-09-16

## What was done
- `app/wol.py`: `is_port_open(host, port, timeout) -> bool` using `socket.create_connection` in a `with`; any `OSError` (refused, timeout, unreachable) → `False`.
- `app/main.py`: `GET /status` returns `{"online": bool, "host": settings.host, "port": settings.status_port}`.
- `tests/test_status.py`: endpoint with the probe patched to True/False, asserting the probe receives the configured host, port, and timeout; `is_port_open` True against a listening loopback socket and False against a closed loopback port.

## Acceptance criteria
| Criterion | Result | Evidence |
|---|---|---|
| `uv run pytest -q` all pass | PASS | 42 passed, 2 warnings (both upstream deprecations) |
| `uv run ruff check .` exits 0 | PASS | "All checks passed!" |
| Smoke: uvicorn probing its own port → `online: true`; probing closed port 1 → `online: false`, both 200 | PASS | curl |

## Note
`/status` performs the TCP connect synchronously inside the request; with the default 1 s timeout the worst-case response time is about 1 s. Acceptable for a single-user server; noted in case it ever matters.
