# T4 — Implement `GET /health` with test (implemented and verified by orchestrator)

- Performed by: orchestrator (Claude Fable 5.1) directly, at the user's request
- Workspace: `/Users/gordonchoi/dev/Projects/gaming-pc-wakeup`
- Completed: 2026-09-14

## What was done
- `app/__init__.py` (empty), `app/main.py` (`app = FastAPI()`, `GET /health` → `{"status": "ok"}`).
- `tests/__init__.py` (empty, makes `app` importable under `uv run pytest`), `tests/test_health.py` (one `TestClient` test).
- No config loading; `/health` has no env dependency.

## Acceptance criteria
| Criterion | Result | Evidence |
|---|---|---|
| `uv run uvicorn app.main:app --port 8765` starts | PASS | log shows "Application startup complete" |
| `curl -s localhost:8765/health` → 200 `{"status":"ok"}` | PASS | body `{"status":"ok"}`, code 200 |
| `/nope` → 404 | PASS | code 404 |
| server stopped afterwards | PASS | process killed, port 8765 free |
| `uv run pytest -q` → 1 passed | PASS | `1 passed, 1 warning in 0.25s` |
| `uv run ruff check .` exits 0 | PASS | "All checks passed!" |

## Note
pytest emits `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.` This is an upstream notice from starlette, not a failure. If it becomes an error in a later starlette release, replace the dev dependency `httpx` with `httpx2` (one-line change in `pyproject.toml`, plus the dependency lists in `SPEC.md` and `.agent/plan.md`).

## Next
T5 (on-device gate) requires the user at the Pi 1 Model B. Set `current_task_status: "waiting_for_human"`.
