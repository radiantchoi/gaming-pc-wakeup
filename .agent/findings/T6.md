# T6 — WoL module (implemented and verified by orchestrator)

- Performed by: orchestrator (Claude Fable 5.1) directly, at the user's request
- Completed: 2026-09-14
- Done ahead of T5 (on-device gate, waiting for the user). Safe because this module is stdlib-only and is reused unchanged under the stdlib contingency.

## What was done
- `app/wol.py`: `parse_mac`, `format_mac`, `build_magic_packet`, `send_magic_packet` (UDP, `SO_BROADCAST`, one `sendto`, socket closed via `with`). Length-checked MAC bytes.
- `tests/test_wol.py`: three input formats (both cases) parse to the same bytes; eight invalid inputs raise `ValueError` (empty, 5 octets, 7 octets, non-hex, 11/13 hex chars, mixed separators, hex with trailing spaces); `format_mac` round-trips; packet is 102 bytes with 6×`0xff` prefix and MAC×16 tail; 5-byte MAC rejected; `send_magic_packet` delivers the packet to a loopback UDP socket.

## Deviation from plan (minor)
The plan said "no network access in tests". One test sends the packet to
`127.0.0.1` on an ephemeral port and reads it back. Loopback only, no
broadcast, consistent with the loopback allowance the plan gives T9.

## Acceptance criteria
| Criterion | Result | Evidence |
|---|---|---|
| `uv run pytest -q` all pass (health + wol) | PASS | `18 passed, 1 warning` (warning is the upstream httpx notice from T4) |
| `uv run ruff check .` exits 0 | PASS | "All checks passed!" |
| No non-loopback network access in tests | PASS | only `127.0.0.1` |
