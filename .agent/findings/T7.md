# T7 — Configuration (implemented and verified by orchestrator)

- Performed by: orchestrator (Claude Fable 5.1) directly, at the user's request
- Completed: 2026-09-14
- Done ahead of T5 (on-device gate, waiting for the user). Safe because this module is stdlib-only and is reused unchanged under the stdlib contingency.

## What was done
- `app/config.py`: frozen `Settings` dataclass and `load_settings(env=os.environ)`. Defaults per `SPEC.md`: broadcast `255.255.255.255`, port `9`, status port `3389`, status timeout `1.0`, token `None`. Missing or empty `WOL_MAC`/`WOL_HOST`, an invalid MAC, or a non-numeric `WOL_PORT`/`WOL_STATUS_PORT`/`WOL_STATUS_TIMEOUT` raise `ValueError` whose message names the variable. Empty `WOL_TOKEN` means no auth.
- `tests/test_config.py`: defaults; custom values parsed to int/float; missing and empty required vars name the variable; invalid MAC names `WOL_MAC`; invalid numbers name their variable; empty token → `None`; default `env` reads the process environment (via `monkeypatch.setenv`).

## Acceptance criteria
| Criterion | Result | Evidence |
|---|---|---|
| `uv run pytest -q` all pass | PASS | `30 passed, 1 warning` (warning is the upstream httpx notice from T4) |
| `uv run ruff check .` exits 0 | PASS | "All checks passed!" |

## Note for T8
`app/main.py` will call `load_settings()` at module level. The `ValueError`
propagates at import, so `uvicorn app.main:app` exits non-zero with the
message in its traceback when `WOL_MAC` is missing.
